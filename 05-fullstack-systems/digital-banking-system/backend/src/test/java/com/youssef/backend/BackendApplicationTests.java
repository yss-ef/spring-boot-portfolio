package com.youssef.backend;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Classe de test principale pour l'application Backend.
 * Vérifie que le contexte Spring se charge correctement.
 */
@SpringBootTest
class BackendApplicationTests {

	/**
	 * Teste le chargement du contexte de l'application.
	 * Si ce test passe, cela signifie que l'application peut démarrer sans erreur de configuration majeure.
	 */
	@Test
	void contextLoads() {
	}

}
